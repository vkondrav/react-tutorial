import ComingSoon from '../ComingSoon';

export default function CSSLesson1_1(): React.ReactElement {
  return (
    <ComingSoon
      lessonId="1.1"
      title="Syntax, Parsing & The DOM"
      module={1}
      moduleTitle="Syntax & Parsing"
    />
  );
}
